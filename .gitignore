.venv/
__pycache__/
*.pyc
.env
raw/

# 실제 질문 원문이 담긴 산출물 — 개인정보 컬럼은 제거했으나 질문 **본문**에
# 이름·소속기관이 그대로 적힌 사례가 확인됐다(예: "○○공단_○○○ 입니다").
# 컬럼 제거·패턴 마스킹으로는 걸러지지 않으므로 원격에 올리지 않는다.
# backend/sheets/ 의 스크립트로 언제든 재생성 가능.
eval/questions.md
eval/goldset-candidates.md

# 초안 검수 리포트 — 질문 원문 포함이라 원격에 올리지 않는다.
eval/draft-review*.md

# 외부 스킬 설치물 — node_modules 와 같은 성격이라 추적하지 않는다.
# skills-lock.json 으로 재설치 가능.
.agents/
.claude/
