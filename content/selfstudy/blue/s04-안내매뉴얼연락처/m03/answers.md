---
anchor_prefix: B-S04-M03
grade: blue
set_no: 4
set_title: 안내매뉴얼연락처
subject_no: 3
subject_title: 서비스구현(자동화)
kind: answers
source: raw/blue/AI 챔피언 블루 인증평가 예제문제/4세트_안내매뉴얼연락처/3과목_서비스구현(자동화)/답안지.pdf
extractor: both
extractors_agree: true
verified: false
---

> **미확정 정답지.** 두 추출기 결과는 일치하지만 아직 사람이 확인하지 않았습니다.
>
> 확정 후 프론트매터의 `verified` 를 `true` 로 바꾸고, 맞는 쪽만 남기세요.

## pypdfium2 추출

<!-- page 1 -->
[연습세트04·블루·3과목] 답안지 (연습 — 공개)
본 과목은 산출물 평가입니다. 단답형 정답이 아닌 ① 정적 페이지 + 외부 리소스(50점) / ② Python 자동화
도구(50점) 두 산출물의 동작·완성도를 채점합니다.
산출물 ① — 정적 페이지 + 외부 리소스 (50점)
항목 배점 채점 기준
카테고리 카드 표시 12 안내서비스.csv를 카테고리별로 묶어 카드 표시
(카테고리명·건수·대표 문의기관)
키워드 검색/필터 10 카테고리 또는 문의기관으로 필터링 + "결과 N건" 화면 표시
외부 리소스(이미지) 임베드 13 첨부 이미지/카드뉴스1~4.png 중 최소 3장 이상 <img> 태그로 직접
노출
배포(Netlify·Vercel·v0) 10 URL 접근 가능 (시크릿 탭 정상 로드, HTTPS)
페이지 제목/구조 기본 5 <title> 적정 명칭, 시멘틱 마크업, 모바일 가독성
산출물 ② — Python 자동화 도구 solution.py (50점)
항목 배점 채점 기준
실행 + 결과 파일 생성 15
$ python solution.py 실행 시 오류 없이 종료,
출력_카테고리별/<카테고리명>.csv 파일 생성,
결과_카테고리집계.csv 생성
카테고리별 행 분리 정확성 15 안내서비스.csv 각 행을 카테고리 컬럼 기준으로
출력_카테고리별/<카테고리명>.csv 에 정확히 분배
집계표 정확성 10 결과_카테고리집계.csv에 카테고리별 건수·대표 문의기관 정확히 기재
콘솔 출력 형식 5
[OK] 안내서비스 N건 카테고리별 분리 → 출력_카테고리별/ / [OK]
집계표 → 결과_카테고리집계.csv 형식
소스 품질 5
표준 라이브러리(csv·pathlib·collections) 사용 권장, 함수화·예외
처리·주석
제출 (100점)
파일 내용
배포_URL.txt 산출물 ① 정적 페이지 URL 한 줄
solution.py 산출물 ② Python 스크립트
결과_카테고리집계.csv Python 실행 결과 집계표 CSV
제출물.md 사용 AI 도구 + 구현 메모 3~5줄

## pdfplumber 추출

<!-- page 1 -->
[연습세트04·블루·3과목] 답안지 (연습 — 공개)
본 과목은 산출물 평가입니다. 단답형 정답이 아닌 ① 정적 페이지 + 외부 리소스(50점) / ② Python 자동화
도구(50점) 두 산출물의 동작·완성도를 채점합니다.
산출물 ① — 정적 페이지 + 외부 리소스 (50점)
항목 배점 채점 기준
안내서비스.csv를 카테고리별로 묶어 카드 표시
카테고리 카드 표시 12
(카테고리명·건수·대표 문의기관)
키워드 검색/필터 10 카테고리 또는 문의기관으로 필터링 + "결과 N건" 화면 표시
첨부 이미지/카드뉴스1~4.png 중 최소 3장 이상 <img> 태그로 직접
외부 리소스(이미지) 임베드 13
노출
배포(Netlify·Vercel·v0) 10 URL 접근 가능 (시크릿 탭 정상 로드, HTTPS)
페이지 제목/구조 기본 5 <title> 적정 명칭, 시멘틱 마크업, 모바일 가독성
산출물 ② — Python 자동화 도구 solution.py (50점)
항목 배점 채점 기준
$ python solution.py 실행 시 오류 없이 종료,
실행 + 결과 파일 생성 15 출력_카테고리별/<카테고리명>.csv 파일 생성,
결과_카테고리집계.csv 생성
안내서비스.csv 각 행을 카테고리 컬럼 기준으로
카테고리별 행 분리 정확성 15
출력_카테고리별/<카테고리명>.csv 에 정확히 분배
집계표 정확성 10 결과_카테고리집계.csv에 카테고리별 건수·대표 문의기관 정확히 기재
[OK] 안내서비스 N건 카테고리별 분리 → 출력_카테고리별/ / [OK]
콘솔 출력 형식 5
집계표 → 결과_카테고리집계.csv 형식
표준 라이브러리(csv·pathlib·collections) 사용 권장, 함수화·예외
소스 품질 5
처리·주석
제출 (100점)
파일 내용
배포_URL.txt 산출물 ① 정적 페이지 URL 한 줄
solution.py 산출물 ② Python 스크립트
결과_카테고리집계.csv Python 실행 결과 집계표 CSV
제출물.md 사용 AI 도구 + 구현 메모 3~5줄
