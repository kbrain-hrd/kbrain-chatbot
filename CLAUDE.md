# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 현재 상태

**로드맵 7단계까지 구현 완료.** 두 유입 경로가 모두 왕복 동작합니다 —
유선(웹 UI)과 시트(폴링 → 슬랙 승인 → 시트 등록).

남은 것은 **사람이 하는 검수**입니다: 답안지 30개 확정, 라인 차이 35건 표본 확인(1단계),
초안 32건 검수(4단계). 실제 운영 시트 적용 전에 `docs/04-open-questions.md` 2-9
(원본 xlsx → 구글 시트 변환)가 해결돼야 합니다.

코드를 작성하기 전에 해당 단계의 설계 문서를 먼저 읽으세요.
설계는 이미 세 번 뒤집혔고(`docs/01-design.md` 2장), 그 이유가 문서에 남아 있습니다.

## 명령어

```bash
uv sync                              # 의존성 설치 (.venv 생성)
uv run python -m backend.ingest.convert   # 1단계: PDF → 마크다운 + 대조 리포트
uv run uvicorn backend.web.app:app        # 5단계: 유선 대응 웹 UI
uv run python -m backend.service          # 6·7단계: 시트 폴링 + 슬랙 승인 (운영 실행)
uv add <pkg>                         # 의존성 추가
```

`.env` 에 `SHEET_URL`·`GOOGLE_CREDENTIALS`·`SLACK_BOT_TOKEN`·`SLACK_APP_TOKEN`·`SLACK_CHANNEL` 이 필요합니다.

### 상시 운영

시트 경로는 **누군가 항상 켜두어야 동작합니다** — 시트를 주기적으로 보고 슬랙 버튼을
기다리는 일이라서요. 그래서 폴링과 리스너를 `backend/service.py` 한 프로세스에 묶었습니다
(폴링은 백그라운드 스레드, 리스너가 메인).

```bash
powershell -ExecutionPolicy Bypass -File install-guard.ps1   # 감시 등록 (한 번만)
powershell -ExecutionPolicy Bypass -File stop.ps1            # 일부러 멈출 때
```

**서비스 창은 뜨지 않습니다.** 작업 스케줄러가 5분마다 `guard.ps1` 을 돌려, 꺼져 있으면
창 없이 다시 띄웁니다. 자동 시작도 이것이 겸합니다.

창을 없앤 이유가 있습니다. **콘솔 창은 사람이 만지면 멈춥니다** — `Ctrl+C` 는 글자가
선택돼 있지 않으면 복사가 아니라 중단이고, 창을 클릭만 해도 선택 모드로 들어가 출력이
막혀 프로그램이 섭니다. 2026-08-07 에 실제로 그렇게 멈춰 **6일 동안 아무도 몰랐습니다.**
`run.bat` 의 재시작 루프는 파이썬이 죽었을 때만 작동해서, 창이 닫히면 그 루프까지
같이 사라집니다.

돌고 있는지 확인: `Get-NetTCPConnection -LocalPort 57321 -State Listen`
로그는 `logs/service.log`, 감시 기록은 `logs/guard.log` (둘 다 git 제외).
감시 해제는 `schtasks /delete /tn "kbrain-chatbot-guard" /f`.

진단이 필요할 때만 따로 띄웁니다.

```bash
uv run python -m backend.sheets.client        # 시트 연결·헤더 확인 + 줄바꿈 서식
uv run python -m backend.sheets.poll --once   # 폴링 1회만
uv run python -m backend.slack.app            # 슬랙 리스너만
```

인제스트는 **멱등**합니다 — 자료가 추가될 때마다 전체를 다시 돌리면 됩니다.
산출물은 `content/selfstudy/`, 대조 리포트는 `eval/ingest-diff.md`.

가상환경 밖에서 `pip` / `python` 직접 실행 금지. 테스트·린트 설정은 아직 없습니다.

## 이 프로젝트가 무엇인가

셀프스터디(AI 챔피언 인증평가) 문제·운영 문의에 대해 **근거 기반 답변 초안을 생성하고
사람 검수를 거쳐 내보내는** 시스템. 완전 자동 발송은 하지 않습니다.

**응시 방식을 알아야 질문이 이해됩니다.** 문제지에 "허용 도구: AI 챗봇 자유 활용"이 명시돼
있어 교육생은 문제지·첨부를 AI에 넣어 풉니다. 그래서 "정답이 뭐예요"는 주력 질문이 아니고,
**"AI로 풀었는데 왜 틀렸나요"** 와 **"이거 문제 오류 아닌가요"** 가 주력입니다.
필요한 근거는 정답 숫자가 아니라 **갈린 지점**(결측치 처리·반올림·경계조건)입니다.

같은 이유로 **해설의 코드는 재현 수단으로서 가치가 낮습니다** — 학생은 그 코드대로 풀지
않았습니다. 반면 해설의 **처리 규칙 서술과 "흔한 오답" 표**는 갈린 지점을 짚는 핵심 자산입니다.
해설을 통째로 취급하지 말고 이 둘을 구분하세요.

질문 유입 경로가 **둘**이고, 코어를 공유합니다:

```
[인제스트] → [앵커 카탈로그] → [질문 분류·문항 특정] → [초안 생성]
                                                            ├── 유선: 웹 UI 즉시 표시 → 담당자 구두 응대
                                                            └── 시트: 슬랙 승인 카드 → 승인 → 시트 등록
```

갈라지는 것은 마지막 출력부뿐입니다. `backend/{ingest,search,answer}` 가 공통 코어,
`backend/web` 과 `backend/{sheets,slack}` 이 그 위에 얹히는 출력부입니다.

- **유선 경로도 승인 게이트를 지킵니다** — 담당자 본인이 실시간 검수자이고, 챗봇 출력이
  사람을 거치지 않고 질문자에게 가는 경로는 없습니다.
- **유선은 응답 시간이 제약입니다** — 통화 중 사용이라, 검수 완료된 답변을 먼저 조회하고
  없을 때만 실시간 생성합니다. 조회 대상은 **승인된 최종답변**이지 미검수 과거 답변이 아닙니다.
- **승인은 슬랙, 수정은 시트** — 슬랙 카드에 초안 전문·근거·플래그·[승인][반려]·시트 링크를
  싣고, 그대로 괜찮으면 슬랙에서 승인, 고칠 게 있으면 시트에서 수정 후 승인.

## 설계상 절대 어기면 안 되는 것

이 제약들은 편의를 위해 완화하면 시스템의 존재 이유가 사라집니다.

1. **문제 원문만이 근거다.** 공식 해설·답안지·과거 답변은 전부 *검수 대상*이지 근거가 아닙니다.
   과거 답변에도, 공식 해설에도 실제로 오류가 있는 것이 확인되어 설계가 두 번 폐기됐습니다.
2. **블라인드 풀이.** Claude에게 해설을 보여주기 **전에** 문제만 주고 먼저 풀게 합니다.
   해설을 함께 주면 앵커링되어 해설의 오류를 못 잡습니다. 풀이 완료 후에만 대조합니다.
3. **삼각 검증.** Claude 독립 풀이 / 공식 해설 / 과거 답변 셋을 대조 →
   전부 일치=초안 생성, 둘만 일치=초안+경고 플래그, 전부 갈림=초안 없이 사람에게.
4. **모르면 초안을 만들지 않는다.** 근거가 없거나 문항 특정이 애매하면 추측하지 말고
   `분류불가` / `근거부족` 플래그로 사람에게 넘깁니다.
   못 답하는 것은 허용, **확신 있는 오답은 불허(목표 0건)**.
   특히 **"이거 문제 오류 아닌가요" 유형은 자동 판정을 내보내지 않습니다** — 대조 결과만
   정리해 사람에게 넘깁니다. 오류라고 잘못 답하면 멀쩡한 자료를 부정하고, 아니라고 잘못
   답하면 실제 오류를 덮습니다.
5. **교재 기준 정답 + 현재 기준 병기.** AI 분야 특성상 교재 시점과 현재가 다를 수 있으나,
   평가가 교재 기준이므로 교재 기준을 정답으로 하고 최신 정보를 덧붙입니다.

## 자료 구조와 앵커

`raw/` 실제 구조 (git 제외):

```
raw/{green,blue}/…/{N}세트_{주제}/{N}과목_{콘텐츠|데이터분석|자동화}/
    문제지.pdf  해설.pdf  답안지.pdf  첨부/
```

= 등급(2) × 세트(5) × 과목(3) = **30개 단위**, 단답 문항 총 100개
(3과목은 단답이 없어 제외: 2과목 × 5세트 × 2등급 × 5문항).

**자료는 계속 추가됩니다.** 인제스트를 일회성 변환 스크립트로 만들지 마세요 — 자료가 들어올
때마다 전체를 다시 돌려도 결과가 같아야 하고(멱등), 앵커 카탈로그도 증분 갱신되어야 합니다.

**앵커 스킴: `G-S01-M02-Q03`** (그린·1세트·2과목·3번), 운영 자료는 `OPS-준비안내-응시환경`.
등급(G/B)은 **필수**입니다 — 그린과 블루는 세트명·주제가 같은데 문제와 정답이 다릅니다.
등급을 빠뜨리면 엉뚱한 정답을 근거로 답하게 됩니다.
(구 문서에 남아 있는 `R01-Q03` 형식은 폐기된 스킴입니다.)

## 기술 선택 중 이유가 있는 것

- **문항은 전량 적재, 운영은 검색.** 문항 100개는 앵커 카탈로그(7.5K 토큰)를 통째로 넣고
  거기서 고르게 합니다. 그린·블루는 세트명·주제·문제 유형이 같고 숫자만 달라 **검색으로
  가르면 엉뚱한 등급을 근거로 답하게 되고**, 주력 동작인 되묻기("양쪽 다 있으니 등급을
  물어야 한다")는 상위 N개 목록으로 표현할 수 없습니다. 카탈로그 최상단의 **⚠️ 공식 정답
  오류 경고가 매 질의 항상 보이는 것**도 전량 적재라서 가능합니다.
  운영 섹션은 사정이 반대입니다 — 등급 개념도 되묻기도 없고 질문↔질문 매칭이라,
  BM25+임베딩 융합으로 상위 5개만 싣습니다 (`backend/search/index.py`). 자료가 쌓여도 프롬프트가
  커지지 않는 것이 도입 이유입니다 (14.4K → 8.4K, 골드셋 정확도는 전량 적재와 동등).
- **운영 검색은 BM25 + 로컬 임베딩 융합입니다.** 둘 중 하나만으로는 안 됩니다.
  BM25는 "떨어지면 다시" ↔ "재응시" 처럼 낱말이 어긋나면 9위까지 밀리고,
  임베딩은 **"75점" 같은 숫자를 놓칩니다**(3위 안에도 못 듦). 점수 스케일이 달라
  더하지 않고 **순위로 융합**합니다(RRF). 모델은 `multilingual-e5-small` 로컬 실행 —
  API 키가 늘지 않고, 반복 실험이 무료이며, 개인정보가 섞일 수 있는 문의 텍스트를
  외부로 내보내지 않습니다.
- **색인만 청킹하고 마크다운은 쪼개지 않습니다.** 섹션 길이 편차가 큽니다
  (문의FAQ 중앙값 94자 ↔ 셀프학습가이드 "실전처럼 연습하기" 20,040자). 긴 섹션을
  벡터 하나로 만들면 의미가 뭉개지므로 색인에서 600자로 나누되(119섹션 → 289청크),
  **앵커는 섹션 그대로 돌려줍니다** — 마크다운을 쪼개면 앵커가 바뀌어 골드셋 레이블과
  문서 참조가 깨지고 사람이 읽는 단위도 무너집니다.
  색인은 **런타임 파생물**입니다 — 원본은 `content/operations/*.md` 이고 파일로 떨구지 않습니다.
- **검색 결과에는 "무관한 것이 섞여 있다"고 명시해야 합니다.** 검색은 관련이 없어도 언제나
  무언가를 돌려줍니다. 실제로 첨부파일 문의에 "교육 신청·접수" FAQ가 그럴듯하게 딸려 와
  문항 문의를 운영으로 오분류시켰습니다. `render_hits` 의 안내 문구가 그 방어선입니다.
- **PDF 추출은 두 추출기 대조가 필수.** 어느 하나를 단일 기준으로 삼으면 정답이 틀립니다 —
  실제로 둘 다 답안지에서 실패하며 **실패 방식이 반대**입니다.
  pdfplumber는 숫자를 **누락**하고(5건), pypdfium2는 좁은 표 열에서 인접 셀 숫자를
  **병합**합니다(2건, `15 4` → `154`). 문제지·해설은 pypdfium2 단일,
  **답안지는 두 결과를 병기하고 사람이 원본과 대조해 확정**합니다.
- **2과목은 코드 실행으로 검증합니다.** 해설 PDF에 정답 산출 전체 코드가 실려 있고 입력 CSV도
  첨부에 있어, 추론이 아니라 실행으로 답을 확인할 수 있습니다(API 비용 없음).
- **답변 생성 모델: Claude Opus 5 (`claude-opus-5`).** 앵커 카탈로그는 매 질의 동일하므로
  프롬프트 **앞쪽에 고정**해 캐싱하고, 질문·날짜 등 가변값은 뒤쪽에 둡니다.

## 3과목은 별도 경로

3과목(자동화)은 단답 정답이 없고 루브릭으로 채점합니다 → **삼각 검증이 성립하지 않습니다.**
질문도 "이렇게 만들면 점수 되나요"에 가까워 근거가 정답이 아니라 채점 기준표입니다.
처리 방식은 아직 미결정이므로 3과목 경로를 임의로 구현하지 마세요(`docs/04-open-questions.md` 2-3).

## 문서

| 문서 | 언제 읽나 |
|---|---|
| `docs/01-design.md` | 신뢰 계층·삼각 검증·워크플로우. 답변 로직 건드리기 전 |
| `docs/02-data.md` | 데이터 소스, 구글시트 스키마·상태 전이 |
| `docs/03-roadmap.md` | 단계별 계획과 **각 단계 완료 조건**, 비용 개산 |
| `docs/04-open-questions.md` | 미결정 사항 — 여기 있는 항목은 임의 결정하지 말 것 |
| `docs/05-data-survey.md` | 실제 자료 실사 결과. 설계 문서 가정과 다른 부분의 최신 근거 |
| `docs/06-operations.md` | **담당자 PC 에 설치해 상시 실행하는 절차.** 문제 대처·비용 |

`docs/03-roadmap.md` 의 단계별 **완료 조건**은 장식이 아닙니다. 각 단계는 완료 조건을
통과해야 다음으로 넘어갑니다(예: 문항 특정 정확도 ≥99%, 변환 시 정답 숫자 누락 0건).

## 공식 정답에 오류가 확인된 문항이 있습니다

`content/catalog.md` 최상단의 **⚠️ 섹션**을 반드시 확인하세요. 현재 5문항
(`B-S05-M01-Q01·Q02`, `G-S05-M01-Q02·Q03·Q04`)은 출제기관이 오류를 인정하고 수정 예정입니다.

**정답을 임의로 정정하지 마세요.** 수정본이 오기 전까지는 판정하지 말고 경위를 안내한 뒤
사람에게 넘깁니다. 답안지 프론트매터의 `official_answer_disputed: true` 가 표시입니다.

이 사례가 중요한 이유: 해당 문항들은 **블라인드 풀이가 답안지와 일치했던** 문항입니다.
풀이가 답안지와 맞는다는 것은 "문제지 해석이 답안지와 같다"는 뜻이지 문제 자체가
타당하다는 뜻이 아닙니다. 응시자 이의 제기가 삼각 검증의 실질적인 네 번째 입력입니다.

## 개인정보

`raw/sheets/` 의 원본 xlsx 에는 교육생 이름·소속기관·연락처·이메일이 있습니다.
**질문·답변만 추출하고 개인정보 컬럼은 읽지 않습니다** (`backend/sheets/extract.py`).
산출물 `eval/questions.md` 는 git 제외입니다 — 본문에 소속기관이 언급될 수 있습니다.

## 문서 충돌 시 우선순위

문서 간 앵커·폴더 구조 불일치는 2026-08-01 정리했습니다. 그래도 충돌이 보이면
**`docs/05-data-survey.md`(실사 결과)가 가장 최신**이고, 결정 사항은
`docs/04-open-questions.md` 3장 확정 테이블이 기준입니다.

`R01-Q03` / `round-NN/` 형식이 어딘가에 남아 있다면 실사 이전의 폐기된 가정이므로
따르지 말고 알려주세요.
