# 구현 계획

## 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 언어·환경 | Python + `uv` | 가상환경 필수 |
| 백엔드 | FastAPI | 유선 UI·시트 경로가 같은 백엔드 사용 |
| **유선 대응 UI** | **웹 (FastAPI 서빙)** | 담당자 여러 명 — CLI 부적합 |
| 시트 연동 | Google Sheets API + Apps Script 트리거 | 질문 입력 즉시 감지 |
| PDF 추출 | **pypdfium2 + pdfplumber 대조** | 둘 다 답안지에서 실패 방식이 다름. 답안지는 병기 후 사람 확정 — [05](05-data-survey.md) 3장 |
| 문서 | PDF → 마크다운 | 변환 검증 포함 |
| 검색 | **문항은 전량 적재, 운영은 하이브리드 검색** | 문항 100개는 목록 전체가 컨텍스트에. 운영은 BM25+임베딩 top-5 — [04](04-open-questions.md) 2-7·2-8 |
| 임베딩 | **로컬 `multilingual-e5-small`** | BM25와 순위 융합(RRF). API 키 불필요 — [04](04-open-questions.md) 2-8 |
| 1차 판정 | Claude Sonnet 5 (`claude-sonnet-5`) | 분류·문항 특정·되묻기. 골드셋 측정으로 확정 |
| 2차 생성 | Claude Opus 5 (`claude-opus-5`) | 블라인드 풀이·삼각 검증. 확신 있는 오답 0건이 걸린 지점 |
| 코드 문제 검증 | 코드 실행 | 추론이 아닌 실측 |
| 알림·승인 | 슬랙 | 초안 카드 + 승인/반려 버튼 — [04](04-open-questions.md) 2-1 |

### 벡터 검색을 쓰지 않는 이유

단답 문항이 100개(2과목 × 5세트 × 2등급 × 5문항)뿐입니다.
전체 문항의 앵커·질문 요약 목록이 수천 토큰이면 컨텍스트에 다 들어갑니다.

목록을 통째로 주고 문항을 특정한 뒤 해당 파일을 로드하면,
**무엇을 근거로 답했는지가 로드 시점에 확정**됩니다.
벡터 검색은 문서를 조각내 "유사한 조각"을 가져오므로 출처가 흐려지고 문맥이 잘립니다.
출처 명시가 이 시스템의 핵심 요구사항이므로 조회 방식이 더 적합합니다.

**단, 이것은 문항에 한한 이야기입니다.** 운영 섹션은 2026-08-03 부터 검색으로
상위 5개만 싣습니다 — 등급 개념도 되묻기도 없어 위 이유가 하나도 해당되지 않고,
자료가 계속 쌓이는 쪽이라 프롬프트 크기를 고정할 실익이 큽니다.

낱말 검색(BM25)과 의미 검색(로컬 임베딩)을 순위로 융합합니다. 한쪽만으로는 부족합니다 —
BM25는 "떨어지면 다시"를 "재응시"와 잇지 못하고, 임베딩은 "75점" 같은 숫자를 놓칩니다.
**색인만 청킹하고 마크다운은 쪼개지 않습니다** — 앵커가 바뀌면 골드셋 레이블과 문서 참조가
깨집니다. 결정 근거와 실측치는 [04-open-questions](04-open-questions.md) 2-7·2-8.

---

## 폴더 구조 (안)

```
kbrain-chatbot/
├── backend/
│   ├── ingest/        # PDF·hwpx·HTML → 마크다운, 추출기 대조 검증
│   ├── search/        # 앵커 카탈로그, 문항 특정 + 조회
│   ├── answer/        # 블라인드 풀이, 코드 실행 검증, 대조, 초안 생성
│   ├── web/           # 유선 대응 웹 UI (FastAPI 라우트 + 프론트)
│   ├── sheets/        # 구글시트 연동
│   └── slack/         # 승인 카드 발송, 버튼 콜백 처리
├── content/
│   ├── selfstudy/     # 등급/세트/과목별 마크다운 + 첨부
│   └── operations/    # 운영 자료 마크다운
├── raw/               # 원본 PDF·hwpx·zip (git 제외)
├── eval/              # 골드셋, 정확도 측정
├── docs/
└── README.md
```

`backend/ingest` → `search` → `answer` 가 **두 경로가 공유하는 코어**이고,
`web` / `sheets`+`slack` 은 그 위에 얹히는 출력부입니다. 갈라지는 것은 마지막 단계뿐입니다.

### 앵커 스킴

```
G-S01-M02-Q03   그린 · 1세트 · 2과목 · 3번 문항
B-S05-M01-Q01   블루 · 5세트 · 1과목 · 1번 문항
OPS-준비안내-응시환경   운영 자료 섹션
```

그린과 블루는 세트명·주제가 같아도 **문제와 정답이 다릅니다.**
등급이 앵커에 없으면 엉뚱한 정답을 근거로 답하게 되므로 필수 항목입니다.

---

## 단계별 계획

각 단계는 **검증 조건을 통과해야 다음으로 넘어갑니다.**

**1~4단계는 두 경로의 공통 코어**입니다. 유선이든 시트든 여기까지는 똑같이 필요합니다.
경로가 갈리는 것은 5단계부터이므로, 그전까지는 순서를 고민할 필요가 없습니다.

### 1단계 — PDF → 마크다운 변환 (90개 PDF)

- pypdfium2로 문제지·해설·답안지 변환
- pdfplumber로 동일 추출 후 **두 결과 대조 → 불일치 지점 리포트**
- 답안지는 두 결과를 병기하고 `verified: false` 로 표시 — 확정 전까지 근거로 쓰지 않음
- 불일치 지점만 사람이 원본과 확인
- **재실행 가능하게(멱등) 작성** — 자료가 계속 추가되므로 일회성 스크립트로 만들지 않습니다

**완료 조건:** 대조 리포트의 모든 불일치 항목 확인 완료. 정답 숫자 누락 0건.
동일 입력으로 재실행 시 산출물이 동일할 것.

**진행 상황 (2026-08-01):** 변환·대조·멱등성은 완료. 헤더 교차검증 0건(배치 사고 없음).
**남은 것은 사람 확인** — 답안지 30개 확정(그중 갈리는 7건 우선), 라인 차이 35건 표본 확인.

### 1.5단계 — 문제지만으로 푸는 검증 (부수 산출물, 선행 가능)

> 당초 계획은 "해설의 정답 산출 코드를 실행해 답안지와 대조"였으나 **교체했습니다.**
> 교육생은 AI로 푸는 것이 정상 응시 방식이라 해설 코드대로 풀지 않습니다.
> 해설 코드를 재현하는 것은 학생 경험과 무관합니다.

**해설을 가리고 문제지 + 첨부파일만 주어 풀게 한 뒤 답안지와 대조합니다.**
학생이 AI로 푸는 것과 같은 조건이므로, 갈리는 문항은 곧 **학생도 틀릴 문항**입니다.

산출물 둘:

1. **"AI로 풀면 다른 답이 나오는 문항" 목록** — 문제지의 규칙이 모호하다는 신호.
   오답 원인 문의가 몰릴 지점을 미리 알 수 있습니다.
2. **답안지 확정** — 두 추출기가 갈린 답안지(1단계 잔여 과제)에서 어느 쪽이 맞는지 판별.
   새 값을 만드는 것이 아니라 **두 추출본 중 택일**하는 용도이므로 순환 논리에 빠지지 않습니다.

세트 간 비교는 하지 않습니다. 각 세트는 독립적으로 판정합니다.

**완료 조건:** 그린·블루 전 세트 1·2과목 대조 완료. 불일치 목록 산출.
1단계에서 갈린 답안지 7건의 확정 근거 확보.

### 2단계 — 문항 단위 분리 및 앵커 부여

- 문항 단위 분리, 앵커 부여 (`G-S01-M02-Q03`)
- 앵커 카탈로그 생성 (앵커 + 질문 요약 + 정답 형식)
- 운영 자료 섹션 앵커 부여

**완료 조건:** 100문항 전부 앵커 부여. 누락·중복 0건. 카탈로그가 컨텍스트 예산 내.

**진행 상황 (2026-08-01): 완료.** 문항 100개 + 운영 섹션 79개, 누락·중복 0건,
약 11.1K 토큰 (`content/catalog.md`). 카탈로그는 **확정된 답안지에서만** 생성한다.

운영 자료 8종: 준비안내(6) · 셀프학습가이드(10) · CBT가이드(21) · 종합안내서(23) ·
비대면교육사전안내(10) · 보조강사FAQ(2) · 운영준비체크리스트(2) · 강사안내메일(5).

문제지 PDF는 표가 평탄화되며 질문이 밀려나거나 중간에서 잘리는 경우가 있어,
자동 추출로 75/100만 건졌다. 나머지는 `content/question-overrides.md` 에 원문 그대로
적어 병합한다. 잘린 질문은 `catalog.py` 가 의심 목록으로 출력해 놓칠 수 없게 했다.

### 3단계 — 질문 분류 + 문항 특정

- 문제 관련 / 운영 관련 / 분류불가 판정
- 등급(그린·블루) 판별 포함 — 질문에 등급이 없으면 되묻기
- 앵커 카탈로그 기반 문항 특정

**완료 조건:**
- **문항 특정 정확도 ≥ 99%** (등급 오판 포함해서 측정)
- 등급 불명 시 추측 없이 되묻는 동작 확인

**진행 상황 (2026-08-01):** 실제 질문 151건으로 골드셋 후보 생성
(`backend/sheets/goldset.py` → `eval/goldset-candidates.md`, git 제외).
분류 체계를 6종으로 재설계했다 — [01](01-design.md) 6장.

측정 시 주의할 점 둘:
- **셀프학습 질문은 검수 전 자료 대상**이라 현재 자료로는 답이 성립하지 않는 건이 있다.
  그대로 측정하면 정확도가 과소평가된다 ([05](05-data-survey.md) 6-3).
- 자동 판정은 **후보**일 뿐이다. 키워드 규칙으로는 한계가 명확하므로
  (질문 분류는 원래 이 단계에서 LLM이 할 일) 사람이 검토해 확정본을 만들어야 한다.

**문항 문의 25건 레이블 확정 완료** (`eval/goldset-labels.md`).
질문 원문 없이 SHA-256 앞 8자로 식별해 커밋한다 — 본문에 이름·소속기관이 적힌 사례가 있다.
`검수전` 4건을 제외한 **21건**이 측정 대상이다.

**등급을 밝힌 질문은 25건 중 2건뿐이었다.** 되묻기가 문항 경로의 기본 동작이어야 하는
실측 근거다 — [01](01-design.md) 6장.

### 측정 결과 (2026-08-01, `claude-sonnet-5`)

| 항목 | 1차 | 2차 | 완료 조건 |
|---|---|---|---|
| 세트·과목 특정 | 53.3% | **100%** (15/15) | ≥99% ✅ |
| 등급 판별 | 33.3% | **100%** (15/15) | ≥99% ✅ |
| 분류 | 90.5% | 90.5% | — |
| 행동 | 38.1% | 81.0% | — |

**완료 조건 충족.** 1차와 2차 사이에 두 가지를 고쳤다.

**골드셋 레이블 10건 정정.** 첫 측정에서 레이블 자체의 오류가 드러났다 — 3과목 6건을
`ask_grade` 로 뒀으나 3과목은 카탈로그에 문항이 없어 되물어도 답할 근거가 없고(`escalate` 가 맞다),
공식정답오류·오류제기 2건도 판정 대상이 아니며, 운영 2건은 CBT가이드·FAQ 에 근거가 있어
`answer` 가 맞았다. **측정 도구가 골드셋의 결함을 드러낸 사례다.**

**등급 추측 금지를 명시.** 등급 판별 33% → 100%를 만든 것은 이 한 가지다.
폴더 경로·과목명 표기(`생성형AI(콘텐츠)` vs `콘텐츠`)·문제 내용은 **등급 근거가 아니라고**
프롬프트에 못 박았다. 그전에는 이런 신호로 `G-`/`B-` 를 임의로 붙였다.

남은 5건 중 3건은 골드셋 쪽도 논쟁 여지가 있는 경계 사례이고, 2건은 모델이 보수적으로
`escalate` 한 것이다. **틀린 답이 아니라 사람에게 넘기는 방향의 실패**이므로
"확신 있는 오답 0건" 기준에 부합한다.

### 실측 비용

건당 **약 12원** (`claude-sonnet-5` 인트로가 기준, 캐시 읽기 20K + 출력 445토큰).
카탈로그가 시스템 규칙 포함 **20K 토큰**이라 당초 예상(12K)보다 크다.

### 4단계 — 초안 생성 + 검증 게이트

- 블라인드 풀이 → 해설·답안지 대조
- 2과목은 코드 실행 결과를 풀이 근거로 사용
- 3과목은 루브릭 안내 경로 (별도 — [04](04-open-questions.md) 2-3)
- 플래그 판정 (해설불일치 / 근거부족)

**완료 조건:** 실제 질문 30건 이상으로 초안 품질 확인. **오답을 확신 있게 제시한 건수 0.**

**진행 상황:** 구현 완료. 자료 보강 후 재실행하여 근거부족이 크게 줄었다. **사람 검수 대기.**

| 항목 | 1차 (08-01) | 2차 (08-03) |
|---|---|---|
| 처리 | 63건 | 66건 |
| 초안 | 30건 | 32건 |
| 신뢰도 | high 1 · medium 13 · low 17 | **high 11 · medium 15 · low 5** |
| 플래그 | 근거부족 26 · 판정보류 1 | **근거부족 19** |
| 플래그 없음 | 3건 | **13건** |

2차 사이에 들어간 것: 운영진확인사항 · 문의FAQ 34개 · 챔피언FAQ 17개
(운영 섹션 79 → 139개). 확신도 high 가 1건에서 11건으로 늘었다.

**남은 근거부족 19건은 응시 환경의 경계 사례에 몰려 있었다.** 자료에는 "모니터 1대만 사용",
"화면 캡처는 문제만 금지" 같은 **원칙**만 있고, 질문은 그 **적용 경계**를 묻는다 —
디스플레이 복제는 1대인가 2대인가, 맥 캡처는 어떤가, 환경준비 2시간 동안 무엇을 하는가.
자료를 더 넣어 풀 문제가 아니라 운영진이 판단해 규칙을 정해야 하는 영역이다.

다섯 건을 운영 담당자가 확인해 주어 `운영진확인사항.md` 에 반영했고,
같은 질문 5건이 전부 정확한 섹션으로 가고 4건이 `high` · 플래그 없음이 되었다.

**매 질의마다 블라인드 풀이를 다시 하지 않는다.** 이 단계를 "블라인드 풀이 → 대조"로 적었으나,
답안지 20개가 실측으로 확정된(`verified: true`) 뒤에는 실시간에 다시 풀 이유가 없다.
느리고 확정본보다 신뢰도가 낮다. 블라인드 풀이는 자료 검증용 배치 작업(1.5단계)이지
응답 경로가 아니다.

**`근거부족` 26건은 실패가 아니다.** 전부 자료에 없는 것을 없다고 밝힌 경우이며,
지어낸 답은 없었다. 다만 이 수치는 **응시자 질문의 상당수가 자료 범위 밖**임을 드러낸다 —
일정 변경, 개인 사정, 서약서 원문 같은 것은 운영진만 아는 내용이라 우리 자료에 없다.

**검수 시 볼 것은 하나다: 근거 없이 단정한 곳.**
`근거부족` · `low` · 되묻기는 정상 동작이다.

### 5단계 — 유선 대응 웹 UI

전화받는 중에 쓰는 화면입니다. **구글시트 없이도 동작하므로 시트 미수령 상태에서 진행 가능**하고,
완성 시점에 그대로 실사용에 들어갑니다.

- 질문 입력 → 초안 + 근거(문항 원문·해설 인용) + 플래그를 한 화면에 표시
- 검수 완료된 답변 우선 조회 → 없을 때만 실시간 생성 ([01](01-design.md) 5장)
- 근거를 접지 않고 바로 보이게 — 담당자가 통화 중에 판단해야 합니다

**완료 조건:** 실제 질문으로 왕복 동작 확인. 검수 완료본 조회 시 즉답.
근거 없이 답을 표시하는 경우 0건.

**진행 상황 (2026-08-01):** 웹 UI 구현 완료. `uv run uvicorn backend.web.app:app`

| 경로 | 응답 시간 |
|---|---|
| 되묻기 (판정만) | 6.0초 |
| 운영 판정 (캐시 적중) | 5.1초 |
| 초안 생성 | 14.1초 |

**판정과 초안을 별도 엔드포인트로 나눴다.** 되묻기가 주력 동작이므로(골드셋 21건 중 17건)
판정만으로 끝나는 경로가 느린 초안 생성을 기다리지 않게 했다. 한 엔드포인트로 처리하면
모든 질문이 초안 대기 시간을 떠안는다.

**미구현: 검수 완료본 우선 조회.** 저장소가 없어 6단계(시트 연동) 이후에 가능하다.
현재는 매번 실시간 생성한다.

**남은 개선:** 초안 14초는 통화 중에 길다. 스트리밍으로 첫 문장부터 보여주면
체감이 크게 줄어든다.

### 6단계 — 구글시트 연동

- 질문 입력 감지 → 초안 기록
- 상태 전이 처리

**완료 조건:** 질문 감지 → 초안 기록 왕복 동작 확인.

### 7단계 — 슬랙 승인 흐름

- 초안 전문 + 근거 + 플래그 + [승인] [반려] + 시트 링크를 담은 슬랙 카드 발송
- 슬랙에서 승인 → 시트에 최종답변 자동 등록, 상태 `완료`
- 수정이 필요하면 시트 링크로 이동 → 수정 후 승인 ([04](04-open-questions.md) 2-1)

**완료 조건:** 승인 → 등록 흐름 정상 동작. 실사용자 테스트 통과.

---

## 검증 기준

| 항목 | 기준 | 이유 |
|---|---|---|
| 문항 특정 정확도 | ≥ 99% | 틀리면 이후 처리가 전부 무의미 |
| 등급(그린·블루) 판별 | ≥ 99% | 같은 세트명·다른 정답이라 혼동 시 오답 직결 |
| 변환 시 정답 숫자 누락 | **0건** | 정답지에서 숫자가 사라지면 근거가 무너짐 |
| 확신 있는 오답 | **0건** | 가장 위험한 실패 유형 |
| 초안 무수정 승인률 | 측정 | 자동화 확대 판단 근거 |
| 유선 응답 시간 | 측정 | 통화 중 사용이라 지연이 곧 실패. 검수 완료본 조회는 즉답 |

**못 답하는 것은 허용, 틀린 답을 확신 있게 제시하는 것은 불허.**

---

## 비용 개산

실제 문서 크기를 확인한 뒤 재산정한 값입니다.
모델은 Claude Opus 5 (`claude-opus-5`) — 입력 $5 / 출력 $25 per 1M tokens 기준.

### 문서 실측 크기

| 문서 | 텍스트 분량 |
|---|---|
| 문제지 1건 | 약 1.3~5.2KB |
| 해설 1건 | 약 1.5~3.7KB |
| 답안지 1건 | 약 1.0~2.1KB |

한 문항에 답하기 위해 로드하는 양은 **한 과목 단위(문제지+해설+답안지) 약 4~11KB**입니다.

### 질의 1건당 — 실측 (2026-08-01)

당초 "건당 100원 내외"로 잡았으나 **과대추정이었다.** 캐싱 미적용에 모든 질의가 블라인드
풀이까지 간다고 가정한 값인데, 실제로는 대부분이 1차 판정에서 끝나고 카탈로그는 캐시에 걸린다.

| 단계 | 모델 | 건당 | 비고 |
|---|---|---|---|
| 1차 판정 (분류·특정·되묻기) | `claude-sonnet-5` | **약 12원** | 실측. 전 질의가 거침 |
| 2차 생성 (블라인드 풀이·대조) | `claude-opus-5` | 약 99원 | 골드셋 기준 8%만 도달 |

**월 200건 기준 약 3,400원.** 유선 경로에서 검수 완료본이 조회되면 API 호출이 아예 없다.

모델 선택은 비용이 아니라 정확도로 정했다 — 조합 간 차이가 월 1천 원 남짓이라
의사결정 요인이 되지 못한다. 1차를 `claude-haiku-4-5` 로 내리는 것은 골드셋에서 99%를
넘는지 확인한 뒤 판단한다(건당 12원 → 약 5원).

### 프롬프트 캐싱

앵커 카탈로그(전 문항 목록)는 모든 질의에서 동일하므로 캐싱 대상입니다.
Opus 5의 최소 캐시 가능 길이는 **512 토큰**이라 카탈로그 크기면 충분히 걸립니다.
캐시 읽기는 입력 정가의 약 10% → 반복 질의에서 입력 비용이 크게 떨어집니다.

캐싱이 깨지지 않으려면 카탈로그를 프롬프트 **앞쪽에 고정**하고,
질문·날짜 등 매번 바뀌는 값은 뒤쪽에 두어야 합니다.

### 변수

- 독립 다중 풀이(같은 문제를 여러 번 풀어 대조) 적용 시 배수 증가
- 2과목 코드 실행 검증은 로컬 실행이라 API 비용 없음
- 운영 질문은 근거가 짧아 더 저렴
- 유선 경로에서 **검수 완료본이 조회되면 API 호출이 아예 없습니다** — 반복 질문이 쌓일수록 평균 단가 하락
